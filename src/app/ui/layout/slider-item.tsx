import React from 'react'

const sliderItem = ({string:[]}) => {
    //tipo para un arrays de imagenes se escribe
    //const images:  = ['image1', 'image2', 'image3']

  return (
    <li>
        slider-item
    </li>
  )
}

export default sliderItem